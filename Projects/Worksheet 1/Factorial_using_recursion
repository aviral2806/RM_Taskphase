#include <iostream>
using namespace std;
int factorial(int n){
    if(n==0){
        return 1;
    }
    return n*factorial(n-1);
}
int main(){
   int n=0;
   cout << "Enter number: ";
   cin >> n;
   int fact=0;
   fact=factorial(n);
   cout << endl << fact << endl;
}
